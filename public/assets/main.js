$( document ).ready(function() {
  console.log( "ready!" );
  $('.nav-login-link').click( () => {
    // Pull down the header and display the login form
  });
  $('.nav-about-link').click( () => {
    // Pull down the header and display the about us information
  });
  $('.logo').click( () => {
    // Reset CSS back to the original home page.
  });
  $('.content')
    // Load's content partials
  $('.triangle-up').click( () => {
    // Pull up the triangle to show information on plants
  });
});
